{
  "name": "localsync-captions",
  "version": "0.1.0",
  "private": true,
  "description": "Privacy-first local caption broadcasting over LAN using Express, SSE, and HTMX.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "qrcode": "^1.5.4",
    "qrcode-generator": "^2.0.4"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1",
    "lighthouse": "^13.0.3"
  }
}
